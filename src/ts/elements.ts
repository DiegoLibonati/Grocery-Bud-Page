export const inputDataEntry = document.querySelector(
  ".section_container_dataentry input"
) as HTMLInputElement;
export const btnAddData = document.querySelector(
  ".section_container_dataentry button"
) as HTMLButtonElement;

export const containerShowItems = document.querySelector(
  ".section_container_items_list"
) as HTMLUListElement;

export const btnClearAllItems = document.querySelector(
  ".section_container_clearitems button"
) as HTMLButtonElement;
